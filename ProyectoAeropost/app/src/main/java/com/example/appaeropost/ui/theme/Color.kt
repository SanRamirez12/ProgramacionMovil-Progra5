package com.example.appaeropost.ui.theme

import androidx.compose.ui.graphics.Color

// ===== Aeropost brand =====
val AeropostBlue       = Color(0xFF2F6FE3) // Primario (azul marca)
val AeropostBlueDark   = Color(0xFF1E4FB3) // Variante más oscura (dark / pressed)
val AeropostBlueLight  = Color(0xFF5C87E8) // Variante clara (accent / containers)

// ===== Neutros / superficies =====
val SurfaceLight       = Color(0xFFFFFFFF) // Superficie clara
val BackgroundLight    = Color(0xFFF6F8FF) // Fondo muy claro con tinte azul
val SurfaceDark        = Color(0xFF121212) // Superficie oscura
val BackgroundDark     = Color(0xFF0C0C0C) // Fondo oscuro

val OnSurfaceLight     = Color(0xFF1B1B1B)
val OnSurfaceDark      = Color(0xFFEDEDED)

val OutlineLight       = Color(0xFFD6DBE8) // Bordes, divisores (light)
val OutlineDark        = Color(0xFF3C4354) // Bordes, divisores (dark)
