<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- cacheDir -->
    <cache-path
        name="cache"
        path="." />

    <!-- filesDir -->
    <files-path
        name="files"
        path="." />

    <!-- getExternalFilesDir(...)   (aquí caen Documents/facturas/...) -->
    <external-files-path
        name="external_files"
        path="." />

    <!-- externalCacheDir -->
    <external-cache-path
        name="external_cache"
        path="." />

</paths>

